class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        return sorted(list(s)) == sorted(list(t))
        """
        t_list = list(t)
        for letter in s:
            if letter in t_list:
                t_list.remove(letter)
            else:
                return False
        if not t_list:
            return True
        else:
            return False
        """
